#include <iostream>
#include <algorithm>

using namespace std;
//Unique number

/*int uniqueocc(int arr[],int size)
{
    std::sort(arr,arr+size);
    for(int i=0;i<size;i=i+2)
    {
        if(arr[i]!=arr[i+1])
            return arr[i];
    }
    return -1;
}

int main()
{
    int arr[100],size;

    cout<<"Enter number of elements: ";
    cin>>size;
    cout<<"Enter elements: ";
    for(int i=0;i<size;i++)
        cin>>arr[i];
    cout<<"\nUnique Element is: "<<uniqueocc(arr,size);

    return 0;
}*/
//------------------------------------------------------

//Unique Number of Occurences


